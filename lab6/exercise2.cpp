#include <iostream>
#include <cstdlib>
#include <time.h>
#include <math.h>
using namespace std;



                double x = (rand() % 10000) / 10000.0;
                //cout << "X: " << x;
                double y = (rand() % 10000) / 10000.0;

double* pi_sequence(const int& n);

int main() {
	srand(time(NULL));
}
